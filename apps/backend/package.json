{
    "name": "backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "source myvenv/bin/activate && uvicorn main:app --reload",
      "start": "uvicorn main:app --host 0.0.0.0",
      "lint": "ruff check .",
      "type-check": "mypy ."
    }
  }